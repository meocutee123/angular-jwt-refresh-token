import { App.DirectiveDirective } from './app.directive.directive';

describe('App.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new App.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
