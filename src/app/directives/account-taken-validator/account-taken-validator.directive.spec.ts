import { AccountTakenValidatorDirective } from './account-taken-validator.directive';

describe('AccountTakenValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountTakenValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
